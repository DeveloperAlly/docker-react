sudo: required
language: generic
services: 
  - docker

before_install:
  - docker build -t allyhaire/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true allyhaire/docker-react npm run test
